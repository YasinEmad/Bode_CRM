# نظام إدارة الفريق (Teams Management System)

## نظرة عامة
نظام متكامل لإدارة فريق المبيعات، يوفر إمكانيات إنشاء وإدارة الفريق للأدمن، وعرض بيانات الفريق لقادة الفريق.

---

## 1. الميزات الرئيسية

### أ. صفحة الأدمن: "Create and Manage Teams"

#### 1.1 عرض الفريق
- **جدول شامل** يعرض جميع الفريق المُنشأة مع الإحصائيات:
  - **اسم الفريق**
  - **اسم قائد الفريق** (مع صورة الملف الشخصي)
  - **عدد الأعضاء** (شارة زرقاء)
  - **إجمالي عدد الليدز** (شارة سماوية)
  - **عدد الليدز النشطة** (ليست "new") (شارة خضراء)
  - **تاريخ الإنشاء**

#### 1.2 إنشاء فريق جديد
عند النقر على زر "Create Team":
- **اسم الفريق**: إدخال نصي (مطلوب - يجب أن يكون فريداً)
- **اختيار قائد الفريق**: 
  - قائمة منسدلة تحتوي فقط على المستخدمين الذين position = "Team Leader"
  - لا يمكن إضافة قائد الفريق كعضو في الفريق
- **إضافة الأعضاء**:
  - تحديد متعدد من الموظفين
  - البحث عن الموظفين حسب الاسم أو اسم المستخدم
  - لا يمكن إضافة قادة الفريق كأعضاء
  - كل موظف يمكن أن ينتمي لفريق واحد فقط

#### 1.3 تعديل الفريق
عند النقر على زر "Edit":
- تعديل اسم الفريق
- تغيير قائد الفريق
- إضافة أو حذف الأعضاء
- جميع التحقق من الصحة ينطبق هنا أيضاً

#### 1.4 حذف الفريق
عند النقر على زر "Delete":
- تأكيد الحذف
- إزالة ارتباط الفريق من جميع الأعضاء

#### 1.5 البحث
- البحث في الفريق حسب:
  - اسم الفريق
  - اسم قائد الفريق

---

### ب. صفحة قائد الفريق: "My Team"

#### 2.1 عرض معلومات الفريق
- **اسم الفريق** في رأس الصفحة

#### 2.2 جدول الأعضاء
يعرض جميع أعضاء الفريق مع:
- **الاسم** (مع اسم المستخدم)
- **الوظيفة** (Position)
- **عدد الليدز الكلي**
- **عدد الليدز المغلقة**
- **معدل التحويل** (Conversion Rate)
  - يظهر كنسبة مئوية مع أيقونة اتجاه
  - يتم حسابه كـ: (عدد الليدز المغلقة ÷ إجمالي عدد الليدز) × 100

#### 2.3 زر "View Leads"
عند النقر على الزر:
- عرض جميع الليدز الخاصة بالموظف
- عرض حالة كل Lead فقط (بدون تعديل)
- الحالات المدعومة:
  - new (أزرق)
  - connected (بنفسجي)
  - negotiation (برتقالي)
  - pending_closed (أصفر)
  - closed_pending_approval (سماوي)
  - closed (أخضر)
  - lost (أحمر)

#### 2.4 زر "View All Team Leads"
عند النقر على الزر:
- عرض جميع الليدز الخاصة بالفريق
- عرض اسم الموظف المسؤول لكل Lead
- عرض حالة كل Lead فقط (بدون تعديل)

#### 2.5 الحظر:
- ✗ لا يمكن تعديل أي بيانات
- ✗ لا يمكن تغيير حالة الموظفين
- ✗ لا يمكن إعادة توزيع الليدز
- ✗ لا يمكن إنشاء أو حذف أي شيء

---

## 2. قواعد النظام

### أ. قواعد الفريق
- اختيار قائد الفريق يكون فقط من المستخدمين الذين `position` = "Team Leader"
- كل موظف ينتمي لفريق واحد فقط
- كل فريق له قائد فريق واحد فقط
- الليدز ترتبط بالفريق تلقائياً حسب الموظف المسؤول
- الأدمن فقط له صلاحية إنشاء وإدارة الفريق

### ب. قواعد الصلاحيات
**الأدمن**:
- ✓ إنشاء فريق جديد
- ✓ تعديل الفريق (الاسم، قائد الفريق، الأعضاء)
- ✓ حذف الفريق
- ✓ عرض جميع الفريق والإحصائيات

**قائد الفريق**:
- ✓ عرض معلومات أعضاء الفريق
- ✓ عرض الليدز الخاصة بالفريق
- ✗ تعديل أي بيانات
- ✗ إنشاء أو حذف أي شيء

---

## 3. حساب معدل التحويل (Conversion Rate)

```
Conversion Rate = (عدد الليدز المغلقة ÷ إجمالي عدد الليدز) × 100
```

### مثال:
- موظف عنده 40 Lead
- 10 منهم Closed
- Conversion Rate = (10 ÷ 40) × 100 = **25%**

### ملاحظات:
- الحساب على جميع البيانات من البداية (cumulative)
- يتم حسابه لكل موظف ويعرض في صفحة My Team

---

## 4. API Endpoints

### أ. Team Management

#### GET `/api/teams`
**الوصول**: Admin فقط
**الرد**:
```json
{
  "teams": [
    {
      "id": "...",
      "name": "Team A",
      "leader": { "id": "...", "name": "Ahmed" },
      "membersCount": 5,
      "leadsCount": 20,
      "nonNewLeadsCount": 15,
      "createdAt": "2024-01-20"
    }
  ]
}
```

#### POST `/api/teams`
**الوصول**: Admin فقط
**الجسم**:
```json
{
  "name": "Team Name",
  "leaderId": "...",
  "memberIds": ["...", "..."]
}
```

#### GET `/api/teams/{id}`
**الوصول**: Admin فقط
**الرد**: معلومات الفريق التفصيلية

#### PUT `/api/teams/{id}`
**الوصول**: Admin فقط
**الجسم**: نفس POST

#### DELETE `/api/teams/{id}`
**الوصول**: Admin فقط

---

### ب. Team Members

#### GET `/api/teams/leaders`
**الوصول**: Admin فقط
**الرد**: قائمة جميع المستخدمين الذين position = "Team Leader"

#### GET `/api/teams/members`
**الوصول**: Admin فقط
**الرد**: قائمة جميع الموظفين (ليسوا قادة فريق)

---

### ج. My Team (Team Leader)

#### GET `/api/teams/my`
**الوصول**: Team Leader فقط
**الرد**:
```json
{
  "team": { "id": "...", "name": "Team A" },
  "members": [
    {
      "id": "...",
      "name": "John",
      "position": "Sales",
      "leadsCount": 10,
      "closedCount": 3,
      "conversionRate": 30.0
    }
  ]
}
```

#### GET `/api/teams/my/leads`
**الوصول**: Team Leader فقط
**الرد**: جميع الليدز الخاصة بالفريق

#### GET `/api/leads?userId={userId}`
**الوصول**: Authenticated
**الرد**: جميع الليدز الخاصة بموظف معين

---

## 5. النماذج (Models)

### Team
```typescript
{
  name: string;           // اسم الفريق (فريد)
  leader: ObjectId;       // معرف قائد الفريق
  members: ObjectId[];    // معرفات الأعضاء
  createdAt: Date;
  updatedAt: Date;
}
```

### User
```typescript
{
  // ... الحقول الموجودة
  teamId?: ObjectId | null;  // معرف الفريق (جديد)
}
```

---

## 6. سير العمل

### A. إنشاء فريق
1. الأدمن يدخل إلى صفحة "Create and Manage Teams"
2. ينقر على زر "Create Team"
3. يملأ الاسم واختيار قائد الفريق والأعضاء
4. ينقر "Create Team"
5. يتم التحقق من:
   - اسم الفريق فريد
   - قائد الفريق له position = "Team Leader"
   - عدم وجود أعضاء لديهم position = "Team Leader"
   - عدم انتماء الأعضاء لفريق آخر
6. يتم حفظ الفريق وتحديث teamId للأعضاء

### B. تعديل فريق
1. الأدمن ينقر زر "Edit" بجانب الفريق
2. يعدل البيانات
3. ينقر "Update Team"
4. يتم نفس التحقق من الصحة
5. يتم تحديث الفريق وتحديث teamId للأعضاء الجدد والقدماء

### C. عرض بيانات الفريق (Team Leader)
1. قائد الفريق يدخل إلى صفحة "My Team"
2. يرى جدول بأعضاء الفريق مع الإحصائيات
3. يمكنه النقر على "View Leads" لرؤية ليدز موظف معين
4. يمكنه النقر على "View All Team Leads" لرؤية جميع ليدز الفريق

---

## 7. الأخطاء الشائعة والحلول

### خطأ: "Team name already exists"
- السبب: اسم الفريق المدخل موجود بالفعل
- الحل: استخدم اسم فريق فريد

### خطأ: "Selected leader must have position 'Team Leader'"
- السبب: قائد الفريق المختار ليس له position = "Team Leader"
- الحل: اختر موظف له position = "Team Leader"

### خطأ: "User {name} already belongs to a team"
- السبب: الموظف المختار ينتمي لفريق آخر بالفعل
- الحل: أزل الموظف من الفريق السابق أولاً

### خطأ: "Team not found or you are not a team leader"
- السبب: المستخدم الحالي ليس قائد فريق
- الحل: تأكد من أن لديك role = "sales" و position = "Team Leader"

---

## 8. واجهة المستخدم

### الألوان والشارات
- **الشارات الزرقاء**: عدد الأعضاء
- **الشارات السماوية**: إجمالي الليدز
- **الشارات الخضراء**: الليدز النشطة
- **الشارات الأرجوانية**: معدل التحويل (إذا كان > 0)

### الحالات الملونة للليدز
- **أزرق**: new
- **بنفسجي**: connected
- **برتقالي**: negotiation
- **أصفر**: pending_closed
- **سماوي**: closed_pending_approval
- **أخضر**: closed
- **أحمر**: lost

---

## 9. الاختبار

### سيناريو 1: إنشاء فريق
1. تسجيل الدخول كأدمن
2. الذهاب إلى "Create and Manage Teams"
3. النقر على "Create Team"
4. ملء البيانات
5. التحقق من أن الفريق يظهر في الجدول

### سيناريو 2: عرض بيانات الفريق (Team Leader)
1. تسجيل الدخول كقائد فريق
2. الذهاب إلى "My Team"
3. التحقق من ظهور أعضاء الفريق
4. النقر على "View Leads"
5. التحقق من ظهور الليدز الخاصة بالموظف

---

## 10. الملفات المتعلقة

```
src/
├── models/
│   ├── Team.ts
│   └── User.ts
├── app/
│   ├── admin/teams/page.tsx
│   ├── sales/my-team/page.tsx
│   └── api/
│       ├── teams/route.ts
│       ├── teams/[id]/route.ts
│       ├── teams/my/route.ts
│       ├── teams/my/leads/route.ts
│       ├── teams/leaders/route.ts
│       └── teams/members/route.ts
└── context/
    └── AuthContext.tsx
```

---

## 11. الملاحظات الإضافية

- النظام يدعم البحث الفوري عن الفريق
- عرض تلقائي للرسائل التنبيهية (Toast) عند الإجراءات
- تحديث فوري للبيانات بعد أي تعديل
- معالجة أخطاء شاملة في جميع الـ API endpoints
