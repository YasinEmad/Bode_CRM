# Attendance Records Feature

## نظرة عامة
تم إضافة صفحة جديدة في لوحة تحكم الإدارة تسمى **Attendance Records** لعرض سجلات الحضور الخاصة بالموظفين بشكل منظم ومفصل.

## المميزات

### 1. عرض البيانات في جداول
- البيانات تظهر في شكل جدول يسهل قراءته
- كل صف يمثل موظف واحد
- كل عمود يمثل يوم من أيام الشهر

### 2. تنقل سهل بين الأشهر
- يمكن اختيار الشهر من قائمة dropdown
- يمكن اختيار السنة مباشرة
- عند اختيار شهر يتم تحميل بيانات الشهر فقط

### 3. المعلومات المعروضة
لكل سجل حضور يتم عرض:
- **اسم الموظف** - في العمود الأول
- **حالة الحضور** - 
  - 🟢 **حضر** (بالأخضر) - إذا حضر في الوقت المحدد
  - 🟠 **متأخر** (بالبرتقالي) - إذا حضر متأخراً
  - 🔴 **لم يحضر** (بالأحمر) - إذا لم يحضر
- **مدة التأخير بالدقائق** - عند التأخر
- **وقت الحضور** - الوقت الفعلي للحضور
- **deviceId** - معرّف الجهاز المستخدم في التسجيل
- **التاريخ** - يتم عرضه من خلال رقم اليوم

### 4. إحصائيات سريعة
- **إجمالي السجلات** - عدد مرات الحضور في الشهر
- **عدد الموظفين** - عدد الموظفين الذين حضروا في الشهر
- **الحاضرون في الوقت** - عدد مرات الحضور في الموقت المحدد
- **المتأخرون** - عدد مرات التأخر

## السلوك التلقائي

### عند تسجيل موظف للحضور (Check-in):
1. يتم إرسال طلب POST إلى `/api/attendance`
2. يتم التحقق من:
   - موقع الموظف (يجب أن يكون بالقرب من المكتب)
   - معرّف الجهاز (يجب أن يتطابق مع الجهاز المسجل)
   - أنه لم يسجل حضوره اليوم من قبل

3. يتم إنشاء سجل جديد في قاعدة البيانات تلقائياً تحتوي على:
   - معرّف الموظف
   - التاريخ والوقت
   - الموقع الجغرافي
   - ما إذا كان متأخراً ومدة التأخير
   - معرّف الجهاز

4. الصفحة تسحب البيانات مباشرة من قاعدة البيانات دون أي إدخال يدوي من الإدارة

## طريقة الوصول
- اذهب إلى **Admin Dashboard**
- اختر **Attendance Records** من القائمة الجانبية أو الرابط السريع
- أو انقر على رابط **Attendance** في شريط التنقل

## Model المستخدمة
```typescript
interface IAttendance {
  userId: ObjectId;        // معرّف الموظف
  date: Date;              // تاريخ الحضور
  checkInTime: Date;       // وقت الحضور
  latitude: number;        // خط العرض
  longitude: number;       // خط الطول
  withinRadius: boolean;   // هل الموقع بالقرب من المكتب
  isLate: boolean;         // هل الموظف متأخر
  lateMinutes: number;     // عدد دقائق التأخير
  deviceId: string;        // معرّف الجهاز
  createdAt: Date;
  updatedAt: Date;
}
```

## API Endpoint
```
GET /api/admin/attendance-records?month=YYYY-MM
```
- يتطلب توكن صحيح
- يتطلب أن يكون المستخدم Admin
- يرجع جميع سجلات الحضور للشهر المحدد مع معلومات الموظف
