# ميزة حساب التأخير - Attendance Time Feature

## الوصف
تم إضافة ميزة جديدة تتيح للإدارة تحديد موعد الحضور المطلوب (مثلاً الساعة 9 صباحاً)، وعند حضور الموظف يتم حساب ما إذا كان متأخراً عن هذا الموعد، مع حساب عدد الدقائق أو الساعات التي تأخر بها.

## المتطلبات المنفذة
✅ الإدارة تحدد موعد الحضور المطلوب
✅ حساب التأخير تلقائياً عند حضور الموظف
✅ عرض مدة التأخير (بالدقائق والساعات)
✅ عرض تنبيهات واضحة للموظف عند الحضور
✅ تسجيل حالة التأخير في السجلات التاريخية

## الملفات المعدلة

### 1. نموذج النظام `src/models/SystemSettings.ts`
**التغييرات:**
- إضافة حقل `attendanceTime: string` (HH:mm format)
- القيمة الافتراضية: "09:00" (9 صباحاً)

### 2. نموذج الحضور `src/models/Attendance.ts`
**التغييرات:**
- إضافة حقل `isLate: boolean` - للتحقق من كون الموظف متأخراً
- إضافة حقل `lateMinutes: number` - عدد الدقائق التأخير

### 3. صفحة إعدادات الإدارة `src/app/admin/settings/page.tsx`
**التغييرات:**
- إضافة حقل input من نوع `time` لتحديد موعد الحضور المطلوب
- عرض رسالة توضيحية تشرح استخدام هذا الحقل

### 4. API الحضور `src/app/api/attendance/route.ts`
**التغييرات:**
- حساب موعد الحضور من إعدادات النظام
- مقارنة وقت حضور الموظف بالموعد المحدد
- حساب عدد الدقائق التأخير
- إرسال بيانات التأخير في الرد

### 5. صفحة حضور الموظف `src/app/sales/attendance/page.tsx`
**التغييرات:**
- عرض رسالة مفصلة تخبر الموظف بمدة تأخره
- عرض التنبيهات بألوان مختلفة:
  - **أخضر**: حضور في الموعد المحدد
  - **برتقالي**: حضور متأخر
  - **أحمر**: حضور خارج نطاق المكتب
- عرض مدة التأخير في السجلات التاريخية

## كيفية الاستخدام

### من جهة الإدارة:
1. اذهب إلى صفحة "الإعدادات" (Settings)
2. في قسم "Attendance Settings"
3. ادخل الموعد المطلوب للحضور (مثلاً: 09:00)
4. احفظ الإعدادات

### من جهة الموظف:
1. عند الحضور، سيحصل على رسالة تخبره:
   - إذا كان في الموعد: "✓ Attendance marked successfully on time!"
   - إذا كان متأخراً: "⏰ You are X minutes late!" أو "⏰ You are X hours and Y minutes late!"
2. يظهر شريط برتقالي في السجلات التاريخية يوضح مدة التأخير

## أمثلة على الرسائل
- "⏰ You are 15 minutes late!"
- "⏰ You are 1 hour and 30 minutes late!"
- "✓ Attendance marked successfully on time!"

## التنسيق في السجلات
```
📅 Fri, Jan 27, 2026
🕐 Check-in: 9:15 AM
⏰ Late by: 15m (أو 1h 30m)
```

## البيانات المحفوظة
عند كل حضور يتم حفظ:
- `isLate`: boolean - هل الموظف متأخر
- `lateMinutes`: number - عدد دقائق التأخير

## ملاحظات تقنية
- التأخير يُحسب بالدقائق (مثلاً: 90 دقيقة = ساعة و 30 دقيقة)
- الموعد المحدد يكون لكل يوم عمل
- النظام يقارن الموقت المحلي للخادم بموعد الحضور المحدد
